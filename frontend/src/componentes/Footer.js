import { Box, Typography } from "@mui/material";

export default function Footer() {
    return (
        <Box component="footer" className='Footer' padding={4}>
            <Typography>Comércios S.A.</Typography>
        </Box>
    );
}